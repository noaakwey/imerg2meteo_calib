# IMERG Precipitation Calibration & Temporal Downscaling

## Описание проекта
Данный репозиторий содержит методику и программный код для калибровки спутниковых оценок осадков NASA IMERG V07 по историческим наземным метеорологическим данным с последующей временной дезагрегацией (Temporal Downscaling). 

### Цель и Задачи
**Цель исследования:** Устранить систематическое структурное занижение экстремальных событий спутниковыми алгоритмами IMERG V07 для получения высокоточных и пространственно-распределенных растров осадков, пригодных для расчета эрозионного потенциала дождей ($R$-фактора в уравнении RUSLE) и максимальной 30-минутной интенсивности ($I_{30}$).

**Задачи:**
1. Разработать строгий алгоритм сопоставления спутниковых (3-часовые суммы) и наземных (наблюдения в синоптические сроки) осадков без использования искусственной интерполяции математическими усреднениями.
2. Реализовать статистическую калибровку на основе `Wet-Day Quantile Mapping (QM)` (выравнивание перцентилей только для дней с осадками > 0) на обучающей выборке (2001–2015 гг.).
3. Внедрить алгоритм линейной экстраполяции для экстремумов, выходящих за 99-й перцентиль, чтобы восстановить пиковую кинетическую энергию (E) дождей (на примере исторического максимума в г. Казань, 2008 г.).
4. Провести кросс-валидацию модели (2016–2021 гг.) с использованием метрик KGE (Kling-Gupta Efficiency) и PBIAS (Percent Bias).
5. Разработать скрипт пакетной временной дезагрегации (Temporal Downscaling), который переносит рассчитанные калибровочные QM-функции на сырые 1-часовые GeoTIFF растры с сохранением формы ливня.

**Объект исследования:** Пространственно-временное распределение атмосферных осадков на Европейской территории РФ по данным спутникового радиометрического зондирования Земли (GPM IMERG V07).

**Предмет исследования:** Математические методы коррекции систематических ошибок (bias correction) и временного даунскейлинга спутниковых данных на основе наземной сети метеорологических наблюдений Росгидромета.

---

## Подробная методика
С полным описанием математической подоплеки, подготовки исходных данных в Google Earth Engine, обоснованиями архитектурных решений загрузчика и результатами распределения ошибок (с графиками KGE и PBIAS) можно ознакомиться в [docs/methodology.md](docs/methodology.md).

---

## 🚀 Как повторить эксперимент

### Шаг 1. Подготовка исходных данных
Для воспроизведения эксперимента вам понадобятся:
1. Архивы с данными `.csv` по станциям (метеоданные).
2. Архивы с `.csv` таблицами выгруженных 3-часовых сумм IMERG из Google Earth Engine.

Распакуйте их строго в следующие директории:
* `data/meteo/срочные данные_осадки/` *(содержит Excel/CSV файлы по метеостанциям)*
* `data/imerg/IMERG_STATIONS_3H/` *(содержит CSV файлы IMERG за каждый год)*

*(Примечание: папка `data/` добавлена в `.gitignore`, поэтому исходных тяжелых данных в репозитории нет, их нужно создать локально).*

Если исходные архивы имеют проблемы с русской кодировкой, воспользуйтесь скриптом в корне `unpack_data.py`.

### Шаг 2. Запуск эксперимента (Обучение и Валидация)
Запустите главный пайплайн:
```bash
python src/main.py
```
**Что произойдет:**
- Загрузятся и "спарятся" (paired) данные со всех метеостанций.
- Скрипт рассчитает функции `Wet-Day Quantile Mapping` для 4 сезонов (по обучающей шкале `2001-2015`).
- Произведется валидация `2016-2021`, рассчитаются метрики KGE и PBIAS.
- **Результат:** В папке `output/calib/` появятся индивидуальные откалиброванные `.csv` таблицы для каждой станции (они понадобятся для растров!). В `output/validation_metrics_test.csv` сохранится сводка ошибок.

### Шаг 3. Анализ и Картинки (Опционально)
Для генерации графиков Boxplot и Scatter plot запустите:
```bash
python src/analyze_results.py
```
Сводные графики сохранятся в `output/plots/`.

---

## 🗺️ Как применить калибровку на растры (Temporal Downscaling)

Обученные модели можно спроецировать на 1-часовые `.tif` растры (выгруженные из Google Earth Engine для $R$-фактора).

### Шаг 1. Подготовка растров
Положите скачанные сырые 1-часовые GeoTIFF растры (например, `IMERG_V07_P1h_mm_2008_Q3_permanent.tif`) в директорию:
* `data/imerg/geotiff_1h/`

*Требование:* Для работы скрипта в папке `output/calib/` **уже должны лежать** результаты калибровки по станциям (Шаг 2 из предыдущего раздела). Скрипт берет их в качестве основы!

### Шаг 2. Запуск даунскейлинга
Откройте файл `src/batch_calibrate_rasters.py`. В самом низу раскомментируйте строчку запуска функции `batch_process_rasters(...)` и запустите скрипт:

```bash
python src/batch_calibrate_rasters.py
```

**Что произойдет:**
1. Скрипт считает координаты (X,Y) всех станций и выстроит структуру ближайшего соседа (KDTree).
2. Разобьёт ваш 1-часовой растр на 3-часовые окна.
3. Соберет сырую 3-часовую сумму, найдет для каждого пикселя ближайшую метеостанцию и применит её функцию QM.
4. Разделит новую (откалиброванную, увеличенную) массу осадков обратно по 3 часам пропорционально тому, как шел ливень в сыром IMERG (сохраняя профиль).
5. **Результат:** В папке `output/geotiff_calib/` появятся `*_qm_calibrated.tif` растры, с которыми можно работать дальше в GIS-системах!
